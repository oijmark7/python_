1.print() 함수
정의 :괄호 안에있는것을 프린트하는것
사용방법:print("사용할말")
2. type(),int(),str(),float()함수
    정의:타입은 모르겠습니다 int 정수 는 함수str은합계?float는 실수
    사용방법:int(),type(),str(),float()로사용한다
3.언덱싱 이란
    정의:위에서 알아본 바와 같이, a[9]라는 것은 a라는 문자열에서 9번위치를 말합니다

4.슬라이싱
    정의:문자열 슬라이싱은 특정 문자열에서 무엇인가를 잘라내는 것


5.replace() 함수
    정의:replace(old, new, [count]) -> replace("찾을값", "바꿀값", [바꿀횟수]
    사용방법:replace는 좌측부터 값을 변경하는데, 우측부터 count만큼 변경하고 싶으면 어떻게 해야하나 해서 text.replace(",", "", -1)라고 실행해봤는데 되지 않았습니다.